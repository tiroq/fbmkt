
# ---- Scraper parameters ----
LAT=13.7563
LON=100.5018
RADIUS_KM=50
CATEGORY=all          # vehicles|motorcycles|all
QUERY=                 # e.g. Honda
MAX_ITEMS=300
DETAILS=true           # true -> second pass on item pages
HEADLESS=1            # 1=headless, 0=headful (в Docker обычно только headless)

# ---- Paths inside containers ----
DB_PATH=/data/db/fb_marketplace.db
STORAGE_STATE_FILE=/data/auth/storage_state.json

# ---- Export options (optional) ----
EXPORT_NEW=false
EXPORT_PRICES=false
EXPORT_PRICES_ITEM=
OUT_PATH=/data/exports/export.csv
